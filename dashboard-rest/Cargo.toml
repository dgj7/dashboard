[package]
name = "dashboard-rest"
version = "0.0.2"
edition = "2024"

[dependencies]
common = { path = "../common" }
rocket = { version = "0.5", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
